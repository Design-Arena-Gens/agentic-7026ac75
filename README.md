## Mobile Arena Target Mapping
- Interactive Next.js dashboard to evaluate mobile arena target assignments and coverage.
- Adjust arena geometry, assign squads, and inspect coverage conflicts in real time.
- Import assignments from clipboard JSON or load curated scenarios instantly.

```bash
npm install
npm run dev     # run locally
npm run build   # production build
npm run start   # serve build
npm test        # run Playwright smoke checks
```
